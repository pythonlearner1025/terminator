// The authored first-person animation clips, in the order an animator reads
// them. Held here, with no engine import, so the range panel can list them
// without loading the view stack.
export const AUTHORED_CLIPS = Object.freeze(['Idle', 'Draw', 'Fire', 'Reload', 'AimIn', 'AimOut', 'AimIdle', 'Sprint', 'Inspect'])
