// Run after Blender exits. ffmpeg is sequential, two threads.
import {readFile,writeFile,mkdir,readdir,stat} from 'node:fs/promises'
import {execFileSync} from 'node:child_process'
import {resolve} from 'node:path'
import {CLIPS,digest,SOURCE} from '../qa/export-contract.mjs'
const out=resolve(process.argv[2]||'tools/blender/revolver-rebuild/generated/animation-videos')
await mkdir(`${out}/posters`,{recursive:true})
const run=(args)=>execFileSync('ffmpeg',['-hide_banner','-loglevel','error','-y','-threads','2','-filter_threads','2',...args],{stdio:'inherit'})
const probe=file=>JSON.parse(execFileSync('ffprobe',['-v','error','-count_frames','-select_streams','v:0','-show_entries','stream=codec_name,pix_fmt,width,height,r_frame_rate,avg_frame_rate,nb_frames,nb_read_frames,duration:format=duration,size','-of','json',file],{encoding:'utf8'}))
const clips=[]
let reelFrames=0
for(const name of CLIPS){
  const evidence=JSON.parse(await readFile(`${out}/evidence/${name}.json`,'utf8'))
  if(!evidence.complete||evidence.probeOnly||!evidence.sourceHashesUnchanged)throw Error('Incomplete capture '+name)
  for(const frame of evidence.frames)if(digest(await readFile(`${out}/${frame.file}`))!==frame.sha256)throw Error('PNG hash mismatch '+frame.file)
  if(evidence.frames.length!==evidence.frameCount)throw Error('Missing frames '+name)
  if(evidence.frames.some((f,i)=>i&&f.renderFrame<=evidence.frames[i-1].renderFrame))throw Error('Output frames were not separately rendered')
  const filter=`drawbox=x=0:y=0:w=iw:h=48:color=0x09151f@0.90:t=fill,drawtext=fontfile=/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf:text='${name}':x=16:y=10:fontsize=21:fontcolor=white,drawtext=fontfile=/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf:text='BLENDER PREVIEW  /  30 FPS':x=w-tw-16:y=19:fontsize=10:fontcolor=0x98c9c9`
  const file=`${name}.mp4`
  run(['-framerate','30','-start_number','0','-i',`${out}/frames/${name}/%04d.png`,'-frames:v',String(evidence.frameCount),'-vf',filter,'-an','-c:v','libx264','-preset','fast','-crf','20','-threads','2','-pix_fmt','yuv420p','-r','30','-movflags','+faststart',`${out}/${file}`])
  const validation=probe(`${out}/${file}`),stream=validation.streams[0]
  if(stream.codec_name!=='h264'||stream.pix_fmt!=='yuv420p'||stream.width!==640||stream.height!==480||stream.r_frame_rate!=='30/1'||Number(stream.nb_read_frames)!==evidence.frameCount||Math.abs(Number(stream.duration)-evidence.frameCount/30)>.00001)throw Error('ffprobe failed '+file)
  // A representative real decoded frame, never an AI-generated poster.
  const posterFrame=15+Math.round((evidence.frameCount-31)*(name==='Draw'?.85:name==='Reload'?.67:.5))
  run(['-i',`${out}/${file}`,'-vf',`select=eq(n\\,${posterFrame})`,'-frames:v','1','-q:v','3',`${out}/posters/${name}.jpg`])
  clips.push({name,file,poster:`posters/${name}.jpg`,authoredDuration:evidence.authoredDuration,frameCount:evidence.frameCount,duration:evidence.frameCount/30,holdFramesEachSide:15,uniqueRenderedFrames:evidence.uniqueFrameHashes,reelStart:reelFrames/30,ffprobe:validation,cameraStates:evidence.cameraStates,evidence:`evidence/${name}.json`})
  reelFrames+=evidence.frameCount
  console.log(`${name}: ${stream.nb_read_frames} frames, ${stream.duration}s, 640x480 H264 yuv420p`)
}
await writeFile(`${out}/evidence/concat.txt`,CLIPS.map(n=>`file '../${n}.mp4'`).join('\n')+'\n')
// MP4 container duration rounding can insert millisecond gaps at joins. Reset
// timestamps from frame number, preserving exactly one output per decoded frame.
run(['-f','concat','-safe','0','-i',`${out}/evidence/concat.txt`,'-vf','setpts=N/(30*TB)','-r','30','-c:v','libx264','-preset','fast','-crf','18','-threads','2','-pix_fmt','yuv420p','-an','-movflags','+faststart',`${out}/Revolver-All-Animations.mp4`])
const reelProbe=probe(`${out}/Revolver-All-Animations.mp4`)
if(Number(reelProbe.streams[0].nb_read_frames)!==reelFrames||Math.abs(Number(reelProbe.streams[0].duration)-reelFrames/30)>.00001)throw Error('Reel ffprobe mismatch')
const near=(a,b)=>a.length===b.length&&a.every((x,i)=>Math.abs(x-b[i])<1e-6)
const aimIn=clips.find(c=>c.name==='AimIn').cameraStates,aimOut=clips.find(c=>c.name==='AimOut').cameraStates
const cameras=clips.flatMap(c=>c.cameraStates.map(s=>s.camera)),first=cameras[0]
const cameraValidation={fixedWorldCamera:cameras.every(c=>near(c.position,first.position)&&near(c.quaternion,first.quaternion)&&Math.abs(c.verticalFov-first.verticalFov)<1e-8),fixedVerticalFov54:cameras.every(c=>Math.abs(c.verticalFov-54)<1e-8),aimInStartsAtAimOutEnd:near(aimIn[0].camera.weaponRootPosition,aimOut.at(-1).camera.weaponRootPosition),aimInEndsAtAimOutStart:near(aimIn.at(-1).camera.weaponRootPosition,aimOut[0].camera.weaponRootPosition),aimActuallyMoves:!near(aimIn[0].camera.weaponRootPosition,aimIn.at(-1).camera.weaponRootPosition),note:'Saved source NLA playback, metadata hip/AimIn/Out root placement matching assemble.render_sample camera setup. Fixed54degree vertical Blender camera. Not gameplay footage.'}
if(Object.values(cameraValidation).some(v=>v===false))throw Error('Camera consistency failed')
const manifest={schema:'revolver-animation-videos/v1',createdAt:new Date().toISOString(),method:'Blender Eevee material previews of approved packed source, selecting saved named NLA tracks on all animated objects. Native60Hz frame_set with subframes at30Hz samples including terminal pose. Every output frame actually rendered, including15 additional hold frames each side. No rebuilding, refitting, source saves, AI frames, motion interpolation, or slideshow substitution. Fixed640x480 player POV,54degree vertical FOV and metadata hip/aim placement. Silent H264/yuv420p. Not gameplay footage, a live FPS benchmark, or normal-input proof. Labels added with ffmpeg after Blender exits.',base:'6975545',sourceCredit:{...SOURCE,adaptation:'Imported arms fitted and retargeted to this revolver; original author and CC BY4.0 retained.'},approvedManifestSha256:JSON.parse(await readFile(`${out}/evidence/Idle.json`)).approvedManifestSha256,assetHashes:JSON.parse(await readFile(`${out}/evidence/Idle.json`)).assetHashes,cameraValidation,fps:30,width:640,height:480,clips,reel:{file:'Revolver-All-Animations.mp4',frameCount:reelFrames,duration:reelFrames/30,ffprobe:reelProbe},outputs:[]}

const media=async file=>`data:video/mp4;base64,${(await readFile(`${out}/${file}`)).toString('base64')}`
manifest.sourceBlendSha256=JSON.parse(await readFile(`${out}/evidence/Idle.json`)).sourceBlendSha256
const poster=async file=>`data:image/jpeg;base64,${(await readFile(`${out}/${file}`)).toString('base64')}`
const descriptions={Idle:'Resting grip · subtle breathing',Draw:'Raise and settle into the grip',Fire:'Hammer strike, recoil and recovery',Reload:'Open, eject, insert, close and return',AimIn:'Hip position to aligned sights',AimOut:'Aligned sights back to hip',AimIdle:'Steady hold through the sights',Sprint:'Authored running pose',Inspect:'A closer look at the revolver'}
async function card(c,index,reel=false){
  const name=reel?'All nine animations':c.name,id=reel?'reel':c.name
  const data=await media(c.file),image=await poster(reel?'posters/Inspect.jpg':c.poster)
  return `<article class="${reel?'reel':'card'}" id="${id}"><div class="card-top"><span class="index">${reel?'COMPLETE REEL':String(index+1).padStart(2,'0')}</span><span>${c.duration.toFixed(3)} s · ${c.frameCount} frames</span></div><video id="v-${id}" aria-label="${name}" controls playsinline preload="none" poster="${image}" src="${data}"></video><div class="card-body"><h${reel?'2':'3'}>${name}</h${reel?'2':'3'}><p>${reel?'The complete sequence, labeled throughout. Select a chapter below.':descriptions[c.name]}</p><div class="controls"><button class="play" data-play="v-${id}">Play</button><button data-restart="v-${id}" aria-label="Restart ${name}">↺ Restart</button><div class="rates" role="group" aria-label="${name} playback speed">${[.25,.5,1].map(rate=>`<button data-video="v-${id}" data-rate="${rate}" aria-pressed="${rate===1}">${rate}×</button>`).join('')}</div><a class="download" href="${data}" download="${c.file}" aria-label="Download ${name} MP4">↓ MP4</a></div>${reel?`<nav class="chapters" aria-label="Reel chapters">${clips.map(c=>`<button data-chapter="${c.reelStart}">${c.name}</button>`).join('')}</nav>`:`<small>Authored ${c.authoredDuration.toFixed(3)} s + endpoint holds</small>`}</div></article>`
}
const html=`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Revolver — Motion Library</title><style>
:root{color-scheme:dark;--bg:#0b1117;--panel:#131e28;--line:#2a3a48;--text:#e8eef1;--muted:#a3b6c1;--mint:#b9f4d5}*{box-sizing:border-box}body{margin:0;background:radial-gradient(ellipse at 90% 0,#203d44 0,transparent 38%),var(--bg);color:var(--text);font:16px/1.5 system-ui,sans-serif}a{color:var(--mint)}button,a{touch-action:manipulation}button{font:inherit;color:inherit;cursor:pointer}button,a.download{border:1px solid var(--line);background:#192632;padding:8px 12px;border-radius:7px;text-decoration:none;font-size:13px}button:hover,a.download:hover{background:#2b4050}button:focus-visible,a:focus-visible,video:focus-visible{outline:3px solid var(--mint);outline-offset:3px}button[aria-pressed=true],button.play{background:var(--mint);color:#0b241d;border-color:var(--mint)}main{max-width:1280px;margin:auto;padding:48px 32px}header{max-width:820px;margin:0 0 38px}.eyebrow{font-size:12px;letter-spacing:.19em;color:var(--mint);font-weight:700}h1{font-size:clamp(44px,7vw,80px);line-height:1.02;letter-spacing:-.065em;margin:18px 0}h1 span{display:block;color:#91a9b6;font-weight:400}header p{max-width:690px;color:var(--muted);font-size:17px}.tags{display:flex;gap:8px;flex-wrap:wrap;margin:24px 0}.tags span{padding:5px 11px;border:1px solid #3c565a;border-radius:30px;font-size:12px;color:#c0d9dd}.section-title{display:flex;align-items:baseline;justify-content:space-between;gap:20px;margin-top:42px}.section-title h2{font-size:25px;letter-spacing:-.03em}.section-title p{font-size:13px;color:var(--muted)}.grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:20px}.card,.reel{overflow:hidden;background:var(--panel);border:1px solid var(--line);border-radius:14px;box-shadow:0 14px 38px #0002}.reel{max-width:1040px}.card-top{display:flex;justify-content:space-between;gap:10px;padding:13px 16px;color:var(--muted);font-size:11px;letter-spacing:.03em}.index{color:var(--mint);font-weight:700}.card video,.reel video{display:block;width:100%;aspect-ratio:4/3;background:#070c10}.reel video{max-height:620px;object-fit:contain}.card-body{padding:17px}.card-body h2,.card-body h3{margin:0;font-size:22px;letter-spacing:-.03em}.card-body p{font-size:13px;color:var(--muted);margin:5px 0 18px;min-height:20px}.controls{display:flex;flex-wrap:wrap;gap:7px;align-items:center}.rates{display:flex;gap:3px}.rates button{padding:8px}.download{margin-left:auto}small{display:block;margin-top:13px;font-size:11px;color:var(--muted)}.chapters{display:flex;flex-wrap:wrap;gap:7px;margin-top:18px}.chapters button{font-size:12px}.method{border-top:1px solid var(--line);margin-top:46px;padding-top:25px;max-width:950px;color:var(--muted);font-size:14px}.method h2{color:var(--text)}.method strong{color:var(--text)}footer{font-size:12px;color:var(--muted);padding-top:25px}.notice{padding:14px 18px;border-left:2px solid var(--mint);background:#152a2c;color:#d5e4e5;font-size:14px}code{overflow-wrap:anywhere;font-size:11px}@media(max-width:1000px){.grid{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:620px){main{padding:28px 16px}.grid{grid-template-columns:1fr}.section-title{display:block}.reel video{max-height:none}}@media(prefers-reduced-motion:reduce){*{scroll-behavior:auto!important}}
</style><main><header><div class="eyebrow">BLENDER PREVIEW / MOTION STUDY 01</div><h1>Revolver.<span>Nine authored motions.</span></h1><p>The approved revolver and DJMaesen arms, rendered from their saved Blender animations. Watch every action, slow it down, or take the MP4s with you.</p><div class="tags"><span>9 animations + complete reel</span><span>640 × 480</span><span>30 fps export</span><span>Silent · offline portable</span></div><div class="notice"><strong>Blender previews · sampled offline.</strong> Every frame is an actual material render of the approved saved source. These are not gameplay footage, a live FPS benchmark, or proof of normal gameplay input.</div></header>${await card(manifest.reel,0,true)}<div class="section-title"><h2>The motion library</h2><p>Play one at a time · 0.25× / 0.5× / 1×</p></div><section class="grid" aria-label="Individual animation clips">${(await Promise.all(clips.map((c,i)=>card(c,i)))).join('')}</section><section class="method"><h2>How these were captured</h2><p>Every frame was rendered with <strong>Blender Eevee</strong> using the materials and named animation tracks in the approved packed source. All animated objects select the same saved clip. Native 60 Hz source frames are evaluated with subframes on a <strong>30 fps output timeline</strong>, including the exact terminal pose. Each clip adds <strong>15 rendered endpoint hold frames before and after</strong> the animation (0.5 seconds each). Exact durations and per-frame sample times are recorded in the manifest.</p><p>The player camera stays fixed at <strong>640 × 480, 54° vertical FOV</strong>, with metadata hip placement and smooth AimIn/AimOut root transitions. Studio area lights and a gray background remain fixed across all clips. The source mesh, rig, materials and animation tracks were not rebuilt, refitted or saved. These are <strong>Blender source previews, not gameplay footage</strong>. Earlier independent QA established source/export pose fidelity at 93 sampled instants.</p><p>No per-pose autozoom, AI frames, motion interpolation, or slideshow substitution. Every output frame, including the endpoint holds, is a separate render. H264/yuv420p MP4s are silent. Labels are added during encoding after Blender exits.</p><p>All ten videos, posters, controls, and downloads are embedded in this HTML; it works without a server or internet. Starting a video pauses every other player. Nothing autoplays. Use Tab to reach controls, Space/Enter to activate a button, and the native video controls to seek.</p><p><strong>Source credit:</strong> <a href="${SOURCE.url}">${SOURCE.title}</a> by <a href="https://sketchfab.com/DJMaesen">DJMaesen</a>, licensed under <a href="${SOURCE.licenseUrl}">CC BY 4.0</a>. Arms were fitted and retargeted to the revolver. Original attribution is retained.</p><details><summary>Frozen asset identity</summary><p>Approved manifest SHA-256<br><code>${manifest.approvedManifestSha256}</code></p><p>Exported glTF SHA-256<br><code>${manifest.assetHashes['revolver-rebuild.gltf']}</code></p><p>Integration base <code>6975545</code> · Source animation rate <code>60 Hz</code>. Per-frame times, hashes, camera states and ffprobe results accompany this page in <code>capture-manifest.json</code> and <code>evidence/</code>.</p></details></section><footer>Revolver motion library · Blender material previews · no audio</footer></main><script>
const videos=[...document.querySelectorAll('video')];
for(const video of videos){video.addEventListener('play',()=>{for(const other of videos)if(other!==video)other.pause();document.querySelector('[data-play="'+video.id+'"]').textContent='Pause'});for(const event of ['pause','ended'])video.addEventListener(event,()=>document.querySelector('[data-play="'+video.id+'"]').textContent='Play')}
for(const button of document.querySelectorAll('[data-play]'))button.addEventListener('click',()=>{const v=document.getElementById(button.dataset.play);v.paused?v.play().catch(()=>{}):v.pause()});
for(const button of document.querySelectorAll('[data-restart]'))button.addEventListener('click',()=>{const v=document.getElementById(button.dataset.restart);v.currentTime=0;v.play().catch(()=>{})});
for(const button of document.querySelectorAll('[data-rate]'))button.addEventListener('click',()=>{const v=document.getElementById(button.dataset.video);v.playbackRate=Number(button.dataset.rate);for(const b of button.parentElement.children)b.setAttribute('aria-pressed',String(b===button))});
for(const button of document.querySelectorAll('[data-chapter]'))button.addEventListener('click',()=>{const v=document.getElementById('v-reel');const seek=()=>{v.currentTime=Number(button.dataset.chapter);v.play().catch(()=>{})};if(v.readyState)seek();else{v.addEventListener('loadedmetadata',seek,{once:true});v.load()}});
document.addEventListener('visibilitychange',()=>{if(document.hidden)for(const v of videos)v.pause()});
</script></html>`
await writeFile(`${out}/index.html`,html)
const outputFiles=['index.html',...clips.flatMap(c=>[c.file,c.poster,c.evidence]),manifest.reel.file]
for(const file of outputFiles){const bytes=await readFile(`${out}/${file}`);manifest.outputs.push({file,bytes:bytes.length,sha256:digest(bytes)})}
manifest.galleryBytes=(await stat(`${out}/index.html`)).size
await writeFile(`${out}/capture-manifest.json`,JSON.stringify(manifest,null,2)+'\n')
await writeFile(`${out}/SHA256SUMS`,[...manifest.outputs.map(f=>`${f.sha256}  ${f.file}`),`${digest(await readFile(`${out}/capture-manifest.json`))}  capture-manifest.json`].join('\n')+'\n')
console.log(`Complete: ${reelFrames} frames, ${reelFrames/30}s reel, ${(manifest.galleryBytes/1048576).toFixed(2)}MiB self-contained gallery`)
